name = "omnirift-relay"
main = "src/index.ts"
compatibility_date = "2024-09-23"
# nodejs_compat exigido pelo @cloudflare/vitest-pool-workers (import de cloudflare:workers no RoomDO)
compatibility_flags = ["nodejs_compat"]

[[durable_objects.bindings]]
name = "ROOMS"
class_name = "RoomDO"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["RoomDO"]
